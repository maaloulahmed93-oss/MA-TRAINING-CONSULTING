import ParticipantSpace from '../components/participant/ParticipantSpace';

const EspaceParticipantPage = () => {
  return (
    <div className="min-h-screen bg-gradient-to-br from-blue-50 via-white to-purple-50">
      <ParticipantSpace />
    </div>
  );
};

export default EspaceParticipantPage;
