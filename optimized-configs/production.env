# Production Environment Variables for MATC System
# Copy these to your deployment platforms

# ===========================================
# FRONTEND (Vercel) Environment Variables
# ===========================================
# Set these in Vercel Dashboard > Project Settings > Environment Variables

VITE_API_BASE_URL=https://matc-backend.onrender.com/api
VITE_APP_NAME=MA-TRAINING-CONSULTING
VITE_APP_VERSION=1.0.0
VITE_APP_DESCRIPTION=Votre partenaire stratégique pour la transformation digitale et le développement des compétences
VITE_DEV_MODE=false
VITE_DEBUG=false

# Optional Analytics
# VITE_GA_TRACKING_ID=GA-XXXXXXXXX
# VITE_HOTJAR_ID=your-hotjar-id

# Social Media Links
# VITE_FACEBOOK_URL=https://facebook.com/ma-training-consulting
# VITE_LINKEDIN_URL=https://linkedin.com/company/ma-training-consulting
# VITE_TWITTER_URL=https://twitter.com/ma_training_consulting

# ===========================================
# ADMIN PANEL (Vercel) Environment Variables
# ===========================================
# Set these in Vercel Dashboard > Project Settings > Environment Variables

VITE_API_BASE_URL=https://matc-backend.onrender.com/api
VITE_APP_NAME=MATC-ADMIN-PANEL
VITE_APP_VERSION=1.0.0
VITE_DEV_MODE=false
VITE_DEBUG=false

# Admin Authentication (if needed)
# VITE_ADMIN_EMAIL=admin@matc.com
# VITE_ADMIN_PASSWORD=secure-admin-password

# ===========================================
# BACKEND (Render) Environment Variables
# ===========================================
# Set these in Render Dashboard > Service Settings > Environment Variables

NODE_ENV=production
PORT=3001

# MongoDB Atlas Connection
MONGODB_URI=mongodb+srv://username:password@cluster0.xxxxx.mongodb.net/matc_db?retryWrites=true&w=majority

# Security
JWT_SECRET=your-super-secure-jwt-secret-key-here-minimum-32-characters
CORS_ORIGIN=https://matrainingconsulting.vercel.app,https://admine-lake.vercel.app

# Rate Limiting
RATE_LIMIT_MAX=1000
RATE_LIMIT_WINDOW_MS=900000

# Logging
LOG_LEVEL=info

# File Upload Configuration
MAX_FILE_SIZE=10485760
UPLOAD_PATH=/opt/render/project/uploads

# Email Configuration (if needed)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password
# FROM_EMAIL=noreply@matc.com

# ===========================================
# MONGODB ATLAS Configuration
# ===========================================
# Configure these in MongoDB Atlas Dashboard

# Database Name: matc_db
# Collections: programs, categories, partners, registrations, etc.
# IP Whitelist: 0.0.0.0/0 (for Render dynamic IPs)
# Database User: matc_admin (with readWrite permissions)

# ===========================================
# SECURITY CHECKLIST
# ===========================================
# ✅ All secrets are environment variables (not in code)
# ✅ HTTPS enforced on all deployments
# ✅ CORS properly configured
# ✅ Rate limiting enabled
# ✅ Database authentication enabled
# ✅ IP whitelisting configured (if applicable)
# ✅ Security headers configured
# ✅ File upload limits set

# ===========================================
# DEPLOYMENT CHECKLIST
# ===========================================
# ✅ Environment variables set in all platforms
# ✅ Database connection strings updated
# ✅ CORS origins include all deployment URLs
# ✅ Build processes tested
# ✅ Health checks configured
# ✅ Monitoring and alerting set up
